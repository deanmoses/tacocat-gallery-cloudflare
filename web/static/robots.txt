# Crawling is allowed in order to stay out of search results: a crawler must fetch a page to see its noindex, and a
# Disallow would hide that and leave bare URLs listed. The AI crawlers to keep out are named in the block Cloudflare
# puts before this, from the zone's robots.txt setting, and Cloudflare keeps that list current.
User-agent: *
Allow: /
