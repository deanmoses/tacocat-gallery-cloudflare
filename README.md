# tacocat-gallery-cloudflare

Prototype to find out whether moving pix.tacocat.com to Cloudflare works. The goals and the vendor comparison are in `docs/plans/Hosting.md` and `docs/plans/HostingDeepDive.md` in the `tacocat-gallery-sam` repo.

The repo is three npm workspaces: `api/` is the Worker, `web/` the SvelteKit front end (see Front end below), and `shared/` the album schema and path helpers both import. One Worker holds every spike: one D1 database, two R2 buckets (originals and derived images), an upload Queue with a dead-letter queue, the Images binding, and a Container for video transcoding. It serves `pix.deanmoses.com`, a custom domain on a Cloudflare zone standing in for tacocat.com, as well as `workers.dev`, so the tacocat.com DNS move is not needed yet.

## Risk register

Timings are [Globalping](https://globalping.io) probes from Paris, Marseille, Baton Rouge and Los Angeles, and `api/scripts/probe.sh` from Berkeley, with the D1 primary in SJC and read replication on. "Worker" is time spent inside the Worker; TTFB adds the visitor's connection.

| #   | Risk                                                                                  | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --- | ------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Worker plus D1 replica read is not under 100 ms from California, Louisiana and France | ✅ Warm album reads take 17 to 70 ms in the Worker (TTFB 41 to 130 ms). ⚠️ A cold replica takes 180 to 280 ms; see #15.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2   | Read-your-writes after an admin save is slow or wrong                                 | ✅ Reads through the Sessions API bookmark see the admin's write, from replicas too. A save costs the round trip to the SJC primary: 80 to 165 ms from LA, 200 to 300 ms from Paris.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 3   | FTS5 triggers do not keep the search index in sync on D1                              | ✅ With an external-content FTS table keyed by integer rowid (`api/migrations/0002_fts_by_rowid.sql`), a save reads 2 rows. Keyed by a column, every write scanned the whole index (#13).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 4   | Time Travel restore leaves the FTS5 table inconsistent                                | ✅ `item` and `item_fts` come back consistent. ⚠️ Restoring to a timestamp can land minutes early; capture a bookmark before anything risky and restore to that.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 5   | No workable D1 backup, because `wrangler d1 export` refuses FTS5                      | ✅ The nightly cron dumps the tables to R2 as JSON. A restore into a fresh D1 is the rows, then `INSERT INTO item_fts(item_fts) VALUES('rebuild')`. The dump should carry its schema version, since a restore maps the dump's columns onto the current schema.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 6   | Images binding cannot reproduce the crop-then-cover thumbnails                        | ✅ With a change: `fit: cover` centres and upscales, unlike SAM's Sharp code, so the Worker computes SAM's source rectangle and output size and asks Images only to `trim` and resize. `trim` coordinates are in the EXIF-oriented frame.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 7   | Images binding mishandles EXIF orientation or HEIC                                    | ✅ EXIF orientation, and 8-bit iPhone HEIC, which is every one of 863 sampled from 2023 to 2026. ❌ 10-bit HEIC and some non-iPhone HEIC fail (errors 9412, 9516). Fallback: `heif-dec` in the existing container writes one full-size JPEG, and every size is derived from that. Some files named `.heic` are JPEGs, so uploads must sniff content. `GET /debug/image/<r2 key>` reproduces the failures.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 8   | Images binding bills something at zero usage, or needs a paid plan                    | ✅ No charge or subscription so far; check the invoice.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 9   | R2 event notification to Queue to Worker is not a usable upload pipeline              | ✅ An upload is processed within seconds. ⚠️ Only S3 API writes raise the event: a file written through the Cloudflare REST API, including `wrangler r2 object put` and the dashboard, is never processed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 10  | Browser presigned PUT to R2 fails on CORS or needs a custom domain                    | ✅ The browser PUTs straight to R2 with a URL the Worker signs, given the bucket CORS rule in `api/r2-cors.json`. The S3 credentials are an account API token: access key is the token id, secret is the SHA-256 of the token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 11  | Video: Stream cannot take iPhone HEVC, or an ffmpeg Container is too awkward          | ✅ ffmpeg in a Container turns iPhone HDR HEVC into a 1080p H.264 MP4 and a poster. `standard-2` is the production size: a 10 s 4K HDR clip takes about 35 s end to end and 0.2% of the monthly container allowance. Output is capped at 1080p because the tone-map dominates the time. The tone-map (hable, `desat=0`) is watchable but warmer and redder than the phone's own rendering. A file ffmpeg rejects becomes an upload error rather than a retry. Wrangler's "Image already exists remotely, skipping push" means the image did not change. Stream is untested.                                                                                                                                                                                                                                                                                                                          |
| 12  | Passkey-only admin login is too much machinery                                        | ✅ Works, on an iPhone too: `/login`, `/invite/<token>` and a signed 30-day session cookie. A passkey is bound to the hostname, so an `Origin` allowlist picks the site, and `wrangler dev` needs `--local-upstream`. A session cannot be revoked early: removing an admin means deleting their passkeys and rotating `SESSION_SECRET`, which logs every admin out. ⚠️ Some networks cache DNS for hours after a nameserver move; expect it for some visitors after the tacocat.com move.                                                                                                                                                                                                                                                                                                                                                                                                            |
| 13  | Free plan fails closed                                                                | ❌ On Workers Free, past 5M D1 rows read in a UTC day every query in the account fails until midnight, so production needs Workers Paid. The upload consumer is idempotent (the version id comes from the event, the row is written before the original, the inbox object is deleted last) with a dead-letter queue; a rejected file becomes a row in `upload_error`, read with `POST /api/errors`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 14  | Upload metadata extraction differs from Lambda                                        | ⚠️ ExifReader reads IPTC in a Worker, but XMP needs `DOMParser`, which Workers lack, so XMP fields need an XML parser passed to ExifReader.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 15  | Reads after hours of idle are not under 100 ms                                        | ⏳ A cron probes album and search reads from four regions after idle gaps of 1 to 9 hours; `npm run probes --workspace api` prints the results. ⚠️ On the first run, Paris's first read after a quiet spell went to the SJC primary (667 ms TTFB). If that repeats, albums need materializing at the edge or replicas kept warm.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 16  | SvelteKit on the Worker is awkward                                                    | ⏳ One Worker serves `web/`'s static build and the API on `pix.deanmoses.com`: `/` and `/2001/06-15` get the app's `index.html` from the asset router without reaching the Worker, `/login` and `/api/*` reach the Worker. `adapter-cloudflare`, needed for SSR, generates a Worker that exports only `fetch`, so the queue, crons and container cannot move into it without a hand-written wrapper; SvelteKit 3 plans to let the app supply its own Worker file (kit #13692). `@cloudflare/vitest-plugin` needs Vitest 4.1 (Vitest 5 in workers-sdk PR #15500), which is why the workspaces run different Vitest majors. The root, year and day album pages read `/api/album/*`, whose body is the `Album` schema in `shared/`, parsed on both sides. Next: a media page, and thumbnails that need the crop and focal point.                                                                        |
| 17  | Edge cache and custom domain do not behave as expected                                | ✅ Stored derivatives cached per colo through the Cache API: 17 to 66 ms warm. ⚠️ A colo's first request goes to R2 in WNAM, 250 to 550 ms from Baton Rouge and Paris, and at this traffic that is the usual case; an R2 custom domain with Tiered Cache was no faster. Cache R2's 404 briefly (`cacheTtlByStatus`), not for the full TTL. Next: a second derived bucket in WEUR.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 18  | Three environments and CI cannot be driven from config                                | ⏳ `infra/` (OpenTofu) declares the zone, bot and cache settings, D1, the buckets, the R2 custom domain, the queue and its notification; `api/wrangler.jsonc` owns the Worker, bindings, routes and crons, and the D1 schema ships as migrations. Provider gaps: no per-category AI crawler policies; R2 custom domains and event notifications cannot be imported; importing D1 needs `ignore_changes` on the location hint. One account API token has to be made by hand, since Wrangler's OAuth login stops at `zone:read`. ⚠️ Adding `routes` switches off `workers.dev` unless `workers_dev` is set. Not built: environments, previews, deploy on push. Planned release: tests, `wrangler versions upload`, apply migrations, smoke-test the preview URL, `wrangler versions deploy`, and `wrangler rollback` to undo. Unverified: whether a Worker holding a Durable Object gets preview URLs. |

## Budget

The Tacocat account is on Workers Paid ($5/month). An experiment that would consume more than 1% of a monthly allowance in the table below, or anything billed outside it, is costed and approved before it runs; smaller ones just report what they used. Record usage before and after: `npx wrangler d1 info tacocat-proto`, run in `api/`, for D1, the dashboard for everything else.

| Service    | Included each month on Workers Paid                                                        | 1%, ask above this                            |
| ---------- | ------------------------------------------------------------------------------------------ | --------------------------------------------- |
| Workers    | 10M requests, 30M CPU-ms                                                                   | 100k requests, 300k CPU-ms                    |
| D1         | 25B rows read, 50M rows written, 5 GB (Free: 5M reads and 100k writes per day)             | 250M rows read, 500k rows written             |
| R2         | 10 GB-month, 1M Class A (writes, lists), 10M Class B (reads)                               | 100 MB-month, 10k Class A, 100k Class B       |
| Queues     | 1M operations                                                                              | 10k operations                                |
| Images     | 5,000 unique transformations                                                               | 50 transformations                            |
| Containers | 375 vCPU-minutes, 25 GiB-hours of memory; billed only while an instance runs               | 3.75 vCPU-minutes: one minute on `standard-4` |
| Stream     | "Images Stream Basic" appeared on the plan after the upgrade; what it charges is unchecked | always ask                                    |

The container is parked on `lite` (1/16 vCPU, 256 MiB), one instance at most, stopping 10 s after its last request, so an idle or accidental start costs almost nothing. `lite` is too small to transcode real video; size it up deliberately, and back down afterwards, for any video test. Tune ffmpeg in local Docker first (`docker build -t tacocat-transcoder api/transcoder`), where iteration is free.

## Running it

The Worker's scripts live in `api/package.json`: run them from `api/`, or from the root with `--workspace api`, as below. So do Wrangler commands, which find `api/wrangler.jsonc` from the directory they run in. `npm run dev --workspace api` needs an `api/.dev.vars` (gitignored) holding the secrets `api/wrangler.jsonc` lists under `secrets.required`. Tests do not need it.

```bash
fnm use  # or nvm use
npm install
npm run db:migrate:local --workspace api
npm run dev --workspace api
```

Deploy with `npm run deploy --workspace api -- --containers-rollout=none`, which leaves the transcoder container untouched.

## Database schema

`api/src/db/schema.ts` is the source of truth for the tables, in [Drizzle](https://orm.drizzle.team). To change one, edit the schema, run `npm run db:generate --workspace api` to write the migration into `api/migrations/`, review the SQL, then `npm run db:migrate:local --workspace api` and, once it works, `npm run db:migrate --workspace api`. drizzle-kit only writes migrations; Wrangler runs them and records which have been applied.

The FTS5 search table and its triggers are raw SQL (`api/migrations/0002_fts_by_rowid.sql`), because Drizzle does not model virtual tables or triggers, and search queries go through Drizzle's `sql` template. `0001` to `0004` predate Drizzle; the `drizzle_baseline` migration only gives drizzle-kit a snapshot of what they created.

## Development

`npm run quality` formats, lints, type-checks and tests.

## Front end

`web/` is the SvelteKit app, an npm workspace of its own. `npm run dev --workspace web` serves it on `localhost:5173`, and `npm run build --workspace web` writes a static single-page app to `web/build`. The Worker serves that build as static assets, so `npm run dev --workspace api` rebuilds it and serves the app and the Worker's routes together on `localhost:8787`, and `npm run deploy --workspace api` rebuilds it before deploying; a bare `wrangler deploy` or `wrangler versions upload` ships whatever `web/build` holds. Those two scripts build `web/` with `--prefix ../web` after clearing `npm_config_workspace`, because npm passes a `--workspace api` flag down to nested npm commands as that variable, where it overrides a `--workspace web` flag. `run_worker_first` in `api/wrangler.jsonc` lists the Worker's routes, and a new one has to be added there.

`api/` runs Vitest 4 and `web/` Vitest 5, each from its own directory. npm hoists `api/`'s Vitest 4 to the root, so a `web/` test dependency whose peer range also accepts Vitest 4 can end up resolving it; `npm ls vitest` shows which each package gets.

## Admin login

`api/scripts/invite.sh "<name>"` prints a one-time invite link for the deployed Worker; add `--local` for `npm run dev --workspace api`. To check the whole flow without a browser, run `node api/scripts/passkey-selftest.ts "$(api/scripts/invite.sh Selftest --local)"` against `npm run dev --workspace api`. Deploying needs a `SESSION_SECRET` Worker secret; locally it comes from `api/.dev.vars`.

## Idle latency probes

The probe cron runs on Cloudflare, not locally. To read the results, run `npm run probes --workspace api`. To try the probe code without waiting for the schedule, run `npx wrangler dev --test-scheduled --enable-containers=false` in `api/` and request `/__scheduled?cron=23+0,1,3,7,15+*+*+*`. That runs the handler locally against the deployed Worker and writes to the local D1.

Requests to Globalping carry the `GLOBALPING_TOKEN` Worker secret (in `api/.dev.vars` locally), which raises Globalping's rate limit from a per-IP one shared with every Worker on the same egress IP.

## Infrastructure

`infra/` holds the OpenTofu config for everything outside the Worker. It uses an account API token named `CLOUDFLARE_TERRAFORM_API_TOKEN` in `api/.dev.vars`, kept out of `CLOUDFLARE_API_TOKEN` because Wrangler would pick that up over the `tacocat` profile. State is local and gitignored.

```bash
cd infra
CLOUDFLARE_API_TOKEN=$(grep '^CLOUDFLARE_TERRAFORM_API_TOKEN=' ../api/.dev.vars | cut -d= -f2-) tofu plan
```

On a new account, R2 has to be enabled once in the dashboard before `tofu apply` can create a bucket.

A wrangler auth profile bound to the repo's directory keeps Wrangler commands here, `api/` included, from reaching any other account:

```bash
npx wrangler auth create tacocat        # choose only the Tacocat account
npx wrangler auth activate tacocat .
```
