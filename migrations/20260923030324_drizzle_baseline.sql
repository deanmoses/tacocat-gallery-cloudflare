-- Baseline for drizzle-kit: item and probe_result already exist, created by 0001 to 0004. The snapshot in meta/
-- records them so later migrations are generated as diffs; there is nothing to run.
SELECT 1;
