# Porting the AWS back end

This is the plan to port the AWS back end to Cloudflare. We're not 100% sure we're migrating yet, but we're seeing that Cloudflare provides an opportunity to improve on AWS in ways other than perf: developer experience, ease of modifying the system, easy of AIs reasoning about and modifying a singe repo rather than AWS's four repos. So rather than a straight least-effort port, let's use this as an opportunity to improve what exists.

The Worker in `api/` becomes the production back end of pix.tacocat.com: every read, write and pipeline the AWS back end in `tacocat-gallery-sam` has, built on what the prototype proved, with the parts of the prototype that were only for measurement kept and the parts that were shortcuts replaced. This page records the decisions, the target design, how each AWS piece maps onto the Worker, and the order the work lands in.

Decided on 2026-09-24. Where this page and the code disagree, the code has moved on and this page is history.

## Goals

- **Every feature the web app has works**, against this Worker, with the web app changed only where the change makes the whole system simpler. The web app was copied from `tacocat-gallery-sveltekit` at commit 8c57e5f of its `claude/getalbum-caching-nextprev-as8yq8` branch, which computes an album's prev and next from its parent and the latest album from the current year, so the API needs neither.
- **Better engineered than the AWS version**, not a like-for-like port: one atomic batch where AWS had a transaction plus hand-written cascades; a write-once object store where AWS copied files on every rename; tests against real local bindings where AWS tested against a deployed stack; layers the linter enforces where AWS documented them.
- **A foundation for decades.** Breaking changes to the API and the database are fine now, since nothing but the prototype uses them; they will not be later.

Not goals of this port, each with its own row in `docs/Risks.md` or its own later plan: the HEIC formats the Images binding rejects, 10-bit and some non-iPhone files, which until the container fallback in Risks #11 is built cannot be uploaded and have to be converted by hand first; sizing the transcoder for real video, moving `tacocat.com`'s nameservers, the external backup of the originals, and copying the real gallery over (sketched at the end).

## Sources

The AWS back end's `main` is the source, not the edge-caching branch. The branch, `claude/getalbum-caching-nextprev-as8yq8`, was never merged; it is one commit that drops prev/next from `getAlbum`, deletes the latest-album endpoint, and builds CloudFront edge caching that `docs/plans/EdgeCachedAlbums.md` in that repo concludes does not pay. `main` is about seventy commits ahead of it, with lint refactors across nearly every file, restructured integration suites, verified auth on reads and the publish guard moved inside the write. The port takes `main`'s logic and applies the branch's contract change by not porting `getPeers`, `getPrevAndNext` and `getLatestAlbum`.

The video support added to AWS in January 2026 left its data model half migrated: every media row has `itemType: 'image'`, only videos carry `mediaType`, and the types say so in comments. The port finishes it, as `shared/` already does: `itemType` is `'album'` or `'media'`, and every media item has `mediaType` `'image'` or `'video'`, which the database enforces.

## Principles

- **The database is the catalog and R2 is write-once.** An original is written exactly once, under a key that never changes, and the `item` row says which version of a media item is current. Every gallery operation, create, edit, publish, rename, set thumbnail, delete, is one D1 batch that touches no object. The only code that writes to a bucket is the upload pipeline; the only code that deletes from one is the nightly purge.
- **A write's conditions are in the statement.** D1 has no interactive transactions: its one atomic unit is `batch()`, whose statements are fixed before any runs, and Drizzle's `transaction()` sends a `BEGIN` that D1 refuses. So a rule that depends on another row is a condition of the statement that writes, an `EXISTS` in the `WHERE` or an `INSERT … SELECT`, the statement's `meta.changes` says whether the rule held, and when it did not, one follow-up read finds out why and gives the `400` its message. The publish guard, the thumbnail's subtree check, the same-extension rename and the album rename are all written this way, and every such subquery gets a rows-read budget test.
- **One origin.** The app, the API, login and the media are served from the site's own hostname: `/api/*`, `/login`, `/i/*`, `/v/*`, `/raw/*`. No `api.`, `auth.` or `img.` subdomain and no Cognito hosted UI on `login.tacocat.com`, so no CORS and no credentials option on a fetch. The one request that leaves the origin is the upload itself: the browser's presigned PUT goes to the account's R2 S3 endpoint, so the media bucket carries a CORS policy, in `infra/`, that allows PUT with a content type from each environment's site origin and from the local dev origin. AWS's S3 bucket had the same.
- **The shapes both sides agree on live in `shared/`**, as valibot schemas: every record, every request body, every response, the error body, the URL builders. The Worker maps rows to them; the app parses responses with them. Neither imports the other.
- **Layers the linter enforces.** Directories under `api/src` with import rules, in the same `no-restricted-imports` mechanism that holds the web app's layers, plus a cycle check.
- **Validate in the database where it can, and as near to it as possible where it cannot.** Every rule about a single row, required fields, no blanks, formats, one type's fields being null for the other, a crop fitting inside its image, is a `CHECK` or `NOT NULL` constraint in `schema.ts`, so no code path, the import endpoint and a hand-run script included, can write a row the rules forbid. What SQLite cannot express in a constraint, rules that look at other rows or inside a JSON array, is checked in `gallery/`: a cross-row rule as a condition of the writing statement, a JSON rule by the shared schema before the statement is built. Rows coming back out are parsed against a schema before anything reads them, as `albums.ts` does now.
- **Tests against real bindings**, per `docs/Testing.md`: D1, R2, the Queue and Images run locally; only the ffmpeg container and third-party HTTP are stood in for.

## Data model

Every table gets `created_at` and `updated_at`, from one shared Drizzle column set, with `updated_at` set by Drizzle's `$onUpdate` hook on every query-builder update and explicitly by the upsert helper. Both come from SQLite's clock: `created_at` by its default and `updated_at` by a hook that returns the same `strftime` expression, never a JavaScript date. A JavaScript timestamp is taken when the statement is built, before the batch runs, and Workers freeze the clock between I/O, so a row created and updated in one batch, as the pipeline does with a new day album and its first thumbnail, would carry an `updated_at` earlier than its `created_at` and fail the check below. API records keep `updatedOn`, the name the app reads.

The `item` table stays as the prototype shaped it, with three changes: the timestamps above, `thumbnail_id` declared as a foreign key to `item.id` with `ON DELETE SET NULL`, so deleting a photo clears it from the albums it fronts, and two indexes: one on `version_id`, so an object's row is one lookup, and one on `thumbnail_id`, because `ON DELETE SET NULL` has SQLite find every row that points at the deleted one, which without an index is a scan of `item` on every media delete. Both get a rows-read case. The natural key stays `(parent_path, item_name)`, which is the URL scheme; `id` is what other rows reference.

| Column                           | Meaning                                                                                                                                                                                                                                                                                                                                                                                                              |
| -------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `id`                             | Integer rowid; the FTS index and `thumbnail_id` key on it                                                                                                                                                                                                                                                                                                                                                            |
| `parent_path`, `item_name`       | `/2001/06-15/` is `06-15` in `/2001/`; `felix.jpg` in `/2001/06-15/`. Unique together. The root is not a row.                                                                                                                                                                                                                                                                                                        |
| `item_type`, `media_type`        | `album` or `media`; `image` or `video` for media and null for an album, by check constraint                                                                                                                                                                                                                                                                                                                          |
| `title`, `description`, `tags`   | Media caption fields. `summary` is an album's one-line caption, since its date is its title. Tags are a JSON array typed `string[]`, as the crop is typed a rectangle: no delimiter to collide with a keyword, and FTS5's tokenizer reads the same words out of the JSON text. Nothing shows, edits or filters by tag today; a tag table earns its place, and its join and its own FTS triggers, when something does |
| `published`                      | Guests see published albums; media shows whenever its album does                                                                                                                                                                                                                                                                                                                                                     |
| `version_id`                     | Which upload of a media item is current, and the key its original and derivatives are stored under                                                                                                                                                                                                                                                                                                                   |
| `width`, `height`                | In the EXIF-oriented frame, the frame every crop is in                                                                                                                                                                                                                                                                                                                                                               |
| `duration_seconds`               | Videos only; the record calls it `duration`                                                                                                                                                                                                                                                                                                                                                                          |
| `thumbnail_id`, `thumbnail_crop` | The media item an album shows as its thumbnail, by row id; the rectangle of a media item its own thumbnail is cut from                                                                                                                                                                                                                                                                                               |

The other tables stay: `user`, `passkey`, `invite`, `spent_challenge` for login; `upload_error` for what the admin UI polls; `probe_result` for the idle-latency probes. One is new:

**`upload`** records every version id the Worker mints, one row per presigned URL: `version_id`, the `parent_path` and `item_name` the item will have, `target_id` and `target_path` when the upload replaces an existing item, `username` of the admin who asked, and `completed_at` once the pipeline has made it an item. Completed rows stay, as the record of who uploaded what and when; rows that never complete leave with their inbox objects in the purge. It is how the pipeline knows what an object in the inbox is for, since the object's key is only its version id; how the pipeline refuses an object nobody presigned; how a replacement can change an item's name and type without the pipeline guessing; and how the purge tells an upload that was never finished from one that was.

### What the database enforces

Every rule below is a named `CHECK` in `schema.ts` and so in the baseline migration, beside the `NOT NULL`, `UNIQUE` and foreign-key constraints. SQLite's `CHECK` has `GLOB` with character classes, `length`, `trim`, `lower` and the `json_*` functions, which covers formats and shapes; it has no regular expressions and no subqueries. A rule that is a `CHECK` needs a table rebuild to change, so they go in complete now, while the tables are empty.

On `item`:

- **Paths.** `parent_path` is `/`, `/YYYY/` or `/YYYY/MM-DD/`. An album in `/` is named `YYYY`; an album in `/YYYY/` is named `MM-DD`; a media item is in a day album and named `something.ext`, one dot, no slash, nothing blank. This is `isGalleryKey` from `shared/`, as a constraint.
- **Types.** `media_type` is set exactly when `item_type` is `media`. That it is `video` exactly when the name's extension is one `shared/` lists as video is a `gallery/` rule, applied at presign and in the pipeline, not a constraint: a constraint would make supporting a new extension a rebuild of `item`.
- **A media item has a file and a size.** `version_id`, `width` and `height` are set, non-blank and positive for media and null for albums. `version_id` is letters, digits, `.`, `_` and `-`, which admits the ids this Worker mints and the ones AWS assigned. `duration_seconds` is set and positive exactly for a video.
- **Captions belong to one type.** `title` and `tags` are null for albums; `summary` is null for media; `description` is allowed on both. A caption that is present is not blank once trimmed.
- **Tags** are a JSON array. That each element is a non-blank string needs a subquery over `json_each`, which a `CHECK` cannot have, so `gallery/` checks it.
- **Published** is `0` or `1`, and `0` for media, whose visibility is their album's.
- **Thumbnails.** `thumbnail_id` is null for media and references `item.id` with `ON DELETE SET NULL`. `thumbnail_crop` is null for albums and, when set, a JSON object whose `x` and `y` are numbers at or above zero and whose `width` and `height` are positive and fit inside the item's `width` and `height`. That the referenced row is a media item inside the album's subtree looks at another row, so it is a condition of the `UPDATE` that sets it.
- **Timestamps.** `created_at` and `updated_at` are in the format the default writes, `YYYY-MM-DDTHH:MM:SS.sssZ`, and `updated_at` is never earlier than `created_at`. The same two constraints apply to every table.

On the others: a `username` is lowercase letters, digits and underscores, starting with a letter, and every column that names one references `user`; `credential_id` and `public_key` are non-blank and `counter` is at or above zero; an invite's `token_hash` is 64 lowercase hex characters; every `expires_at`, `used_at` and `last_used_at` has the timestamp format; an `upload_error` names a media path and has a non-blank message; an `upload` has a version id of the same shape as `item`'s, a day album path and a media name, a `target_path` that is null exactly when the upload is not a replacement, a `target_id` that is null or references `item.id` with `ON DELETE SET NULL`, so a replacement whose target was deleted before the upload finished is still known to be one, a `username` that references `user`, and a `completed_at` that is null or a timestamp; a `probe_result` has a status of null or 100 to 599, timings of null or at or above zero, and `seq` at or above zero.

Rules with a user-facing answer stay in `gallery/`, where a violation becomes a `400` with a message: a day album publishes only if its year is published, a rename keeps its extension, a media name's extension says its type. A constraint failing is a bug, and answers `500`, except through `PUT /api/item`, the import endpoint, which answers `400` with the constraint's name, so a script feeding it learns which rule a row broke.

**The migrations are reset to one baseline.** Both environments hold prototype data, and the seventeen migrations so far rebuild `item` three times. `api/migrations/` starts over with one generated migration for the schema, one raw-SQL migration for the FTS5 table and its triggers, and one for the seed users. The update trigger fires only on `UPDATE OF item_name, title, description, tags, summary`, the columns the index holds, so setting a thumbnail, publishing, pointing a row at a new version or moving an album's children on a rename leaves the index alone; the rows-read test holds each of those writes to the row and its index entries, since the trigger's cost shows in rows written. Each environment's database is emptied by hand, tables and Wrangler's migration record together, before the branch is pushed to staging and again before the merge that releases production, since the release applies whatever migrations the tree holds. Everything in them goes: the passkeys, so every admin registers again through a new invite, and the probe history, whose numbers are already in `docs/Perf.md`. Right before each wipe, `POST /api/backup` writes the rows to `backups/d1/`, and `docs/Perf.md` notes the date the probe table started over. The release's check that the previous version still works on the migrated database will fail on that push, since the previous version's queries meet a schema they were not written for. The script releases anyway but exits non-zero, so that push's Deploy run shows as failed although the new version is serving; and if the new version fails its own check on the same push, the script leaves the broken previous version serving, so that push is watched rather than left to run.

## Storage

Two buckets, as now: the media bucket holds uploads and originals, the derived bucket holds everything made from them.

| Key                                     | Bucket  | Written by                                         |
| --------------------------------------- | ------- | -------------------------------------------------- |
| `inbox/<versionId>`                     | media   | The browser, through a presigned PUT               |
| `originals/<versionId>`                 | media   | The upload pipeline, once                          |
| `derived/<versionId>/<size>[-crop]-fmt` | derived | The image route, on the first request for a size   |
| `derived/<versionId>/video.mp4`         | derived | The transcoder                                     |
| `derived/<versionId>/poster.jpg`        | derived | The transcoder; the source of a video's thumbnails |
| `backups/d1/<timestamp>.json`           | media   | The nightly backup                                 |

**The gallery path is not in the key.** A `versionId` is unique to one upload, so the object needs nothing else, and the row is what says which path a version belongs to. That is what makes a rename, of a photo or of an album with photos in it, a database change alone, where AWS copied every original and every derivative, one object at a time. It also stops forbidding two things AWS never had, moving a photo between albums and renaming a year, which the app does not offer yet. The URL the app uses keeps the path, `/i/2001/06-15/felix.jpg/<versionId>?size=200x200`, though the Worker needs only the version to find the object and reads no row to serve an image. The path is there for whoever reads the URL: in the network tab, the logs, the probe rows and the browser runs, and in the counting of album views by the `/i/<year>/<day>/` prefix that `docs/Perf.md` inherits from the AWS analyses. It also gives `/raw/` the file name for `Content-Disposition` without a lookup. The Worker never checks that the path matches the row, so a URL cached at the edge, in the app's IndexedDB or in an email keeps working after a rename; `/i/anything/<versionId>` serves the image, and the version is the secret.

What is given up is browsing the bucket by path in the dashboard, and the mitigations are part of the port: `api/scripts/media.ts <gallery path>` prints an item's row and its keys, the `version_id` index answers the reverse question, and each original carries its upload-time path as custom metadata, a label written once and never updated, so a stray object can say where it came from. A listing of `originals/` is in upload order for what this Worker uploaded, since the ids it mints begin with a timestamp; the ids copied from AWS are opaque and sort as they sort.

**The version id is minted when the presigned URL is issued**, time-sortable and URL-safe, written to the `upload` table with what the upload is for, and made the inbox key, so the app learns it from the presign response and the pipeline reads it from the R2 event and looks the rest up. The URL signs no content type, as AWS's did not, so the browser sends the file's own type and the original keeps it. A redelivered event names the same key, finds the upload already completed, and does nothing.

**Any file may replace any media item.** AWS refused a replacement in another format, a PNG over a JPEG or a JPEG over a HEIC, because an item's identity was its path with the extension in it, and the object's key was that path: the bytes and the name would have disagreed, or the captions would have been lost to a new item. Here the row's identity is `id` and the object's is `versionId`, and albums point at a thumbnail by row id, so a replacement in another format is a replacement and a rename in one batch. The same row gets the new version, size, media type and duration, and its name becomes its own base name with the source's extension, `felix.jpg` to `felix.png`, or to `felix.mov` with the transcoder running as for any video. Captions, tags and every album that shows the item carry over, because none of them named the file. Since the name changes with the type, the rule that the name says the type holds throughout. The one thing that cannot carry over unexamined is the crop, which is pixels of the old image: the batch keeps it only when the new image has exactly the old dimensions, the re-export case, and clears it otherwise, since a rectangle chosen on one photo that happens to fit inside another is a plausible-looking wrong thumbnail, worse than none. The rule applies to every replacement; AWS had none because nothing checked. A new name that another item already holds is refused at presign, and refused again by the pipeline if the collision arrived in between. The media page's URL changes with the name, as it does on any rename; every image URL keeps working, since those are keyed by version.

**Nothing is deleted when an admin deletes or replaces.** The row goes, or points at the new version, and the old objects wait for the nightly purge, which lists both buckets, keeps every version some row references, and deletes the rest once they are 30 days old by R2's own upload time. Restoring a deleted or replaced photo within that month is pointing a row at the old version. The purge is the one destructive job, so it deletes at most 500 objects in a run, a day album's worth of replaced originals with their derivatives, and logs what it left for the next night, which turns a bug that orphaned the gallery into a log line rather than a loss. The D1 dumps under `backups/d1/` are referenced by no row, so the same rule is their retention: the newest 30 days of nightly dumps are kept. It also sweeps the leftovers of uploads that failed part way, and inbox objects whose `upload` row is a day old and never completed, the uploads a browser abandoned after taking its URL, deleting those rows with them.

**Originals are kept as uploaded**, HEIC included. The Images binding decodes HEIC itself, so a HEIC's thumbnail and detail image are cut from it like any other image's, once each and stored, and nothing is converted on the way in. The one place a browser meets the file itself is the app's full-size link, and only Safari can show a HEIC, so for a HEIC original `/raw/` answers with a quality-92 JPEG, metadata kept, made by the Images binding on each request and not stored, since the link is rarely followed and a stored copy would double every HEIC's footprint; `?format=original` gives the file itself. When the binding refuses the file, as it does the 10-bit HEICs in Risks #11, `/raw/` answers with the original bytes instead, so the link still works in Safari and fails nowhere else any worse than it did. AWS converted every HEIC to a JPEG on upload with a hand-built Sharp layer, renamed it and deleted the HEIC, which took HEIC-specific code at every step in the back end and the app; here there are two pieces, that branch in `/raw/` and the thumbnail cut at the end of the upload pipeline that catches a file the binding cannot decode.

## The API

Same origin, JSON, under `/api/`. Every write needs a logged-in admin and answers `401 { "errorMessage": "Unauthorized" }` without one. Reads never refuse: a guest gets the published view and the `x-auth-status` header says which view was served. Every error is `{ "errorMessage": string }` from `shared/`'s error schema, the 404s included, and an unexpected exception is `500 { "errorMessage": "Server Error" }` with the detail in the log, never in the body. The method is dispatched, so a `HEAD`, `PATCH` or `DELETE` reaches its handler.

**Routing is Hono's.** The prototype's chain of `startsWith` checks in `index.ts` dispatched three methods and no path parameters; the table above needs every method, parameters that contain slashes, and one error body from every failure, which is what a router is for. Hono is the one built for Workers: its `fetch` has the Worker's own signature, so it sits in the default export beside the queue and cron handlers and the test helpers call the Worker as they do now; its `onError` and `notFound` hooks are where the error shape lives; and its middleware carries what every response gets, the auth-status and timing headers today and the security and robots headers below, and the admin gate on writes. It stays inside `index.ts` and `routes/`: `gallery/` and the layers under it take plain arguments and return data, never Hono's context, which the import rules hold. Settled when it went in: Hono answers a `HEAD` by dispatching it as a `GET` and dropping the body, before any route is matched, so a `HEAD` route cannot be registered; the raw request keeps its method, so the album and media `GET` handlers read it and answer a `HEAD` with an existence check rather than a full read. Bodies are parsed with `safeParse` at the top of a handler, as before, since every schema is in `shared/` already and the validator would add a second way to say the same thing.

| Route                                   | Body → response                                                                                                                                                                                                                                                                                           |
| --------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `GET /api/album/<path>`                 | → the album with its children, no prev/next, as now. Reads through the D1 session bookmark the write set, so an admin sees their own save                                                                                                                                                                 |
| `HEAD /api/album/<path>`                | → 200 or 404, the view the caller may see. The app asks before creating or renaming                                                                                                                                                                                                                       |
| `HEAD /api/media/<path>`                | → 200 or 404                                                                                                                                                                                                                                                                                              |
| `GET /api/search/<terms>?…`             | `oldest`, `newest` (years), `oldestFirst`, `startAt`, `pageSize` → `{ total, items: GalleryRecord[] }`, full records so the app can draw thumbnails. FTS5 matches; the year filter and the sort are on the gallery path, which is chronological. Guests get published items only                          |
| `PUT /api/album/<path>`                 | `{ description?, summary?, published? }`, or no body, which is what the app sends → creates a year or day album; 400 if it exists                                                                                                                                                                         |
| `PATCH /api/album/<path>`               | Same fields, partial; a day album publishes only if its year is published, a condition of the update                                                                                                                                                                                                      |
| `DELETE /api/album/<path>`              | → 400 unless the album is empty                                                                                                                                                                                                                                                                           |
| `POST /api/album-rename/<path>`         | `{ newName }` → renames a day album within its year, the album and every child in one batch, no cap                                                                                                                                                                                                       |
| `PATCH /api/album-thumb/<path>`         | `{ mediaPath }` → the album shows that media item, which may be a grandchild                                                                                                                                                                                                                              |
| `PATCH /api/media/<path>`               | `{ title?, description? }` partial                                                                                                                                                                                                                                                                        |
| `DELETE /api/media/<path>`              | → drops the row; the foreign key clears it from any album's thumbnail; the objects wait for the purge                                                                                                                                                                                                     |
| `POST /api/media-rename/<path>`         | `{ newName }` → same extension, strict file name, one row update                                                                                                                                                                                                                                          |
| `PATCH /api/thumb/<path>`               | `{ x, y, width, height }` in percent of the image → stored in pixels of the EXIF-oriented frame                                                                                                                                                                                                           |
| `POST /api/presigned/<albumPath>`       | `[{ path, replaces? }, …]` → `{ "<path>": { url, versionId } }`. Each `path` is the item the upload will be, in this day album, which must exist; `replaces` names the item it replaces, whose base name `path` keeps with the new file's extension. Each entry becomes an `upload` row                   |
| `POST /api/errors`                      | `{ paths }` → `{ errors: { <path>: message } }` from the last day, as now                                                                                                                                                                                                                                 |
| `PUT /api/item`                         | The whole row of one item, strict. The import endpoint: what `import-album.ts`, the gallery migration and the e2e seed write with, since no other route sets a version id and dimensions                                                                                                                  |
| `GET /api/auth/status`, `/api/auth/*`   | Unchanged                                                                                                                                                                                                                                                                                                 |
| `GET /api/health`, `POST /api/backup`   | Unchanged                                                                                                                                                                                                                                                                                                 |
| `GET /i/<path>/<versionId>?size=&crop=` | Unchanged for readers; the object comes from `derived/<versionId>/`, generated from `originals/<versionId>` or the poster                                                                                                                                                                                 |
| `GET /raw/<path>/<versionId>`           | The original, from `originals/<versionId>`. A HEIC comes back as a full-size JPEG unless `?format=original` asks for the file itself or the binding cannot decode it. The URL loses the `originals` segment it has today; `originalUrl` in `shared/urls.ts` changes and the app, which calls it, does not |
| `GET /v/<path>/<versionId>`             | The MP4 with byte ranges, from `derived/<versionId>/video.mp4`                                                                                                                                                                                                                                            |

The three media routes are not gated on who is asking, as the AWS image CDN was not: the version id is the capability, 64 bits of hash behind a timestamp for the ids this Worker mints and AWS's opaque 32 characters for the copied ones, and knowing it is knowing the photo. Gating `/raw/` and `/v/` on the item's album being published would add a row read to routes that otherwise read none, and would not protect `/i/` at its largest size, which needs to read none. What the routes can reach is fixed by construction instead: `/raw/` reads only `originals/<versionId>` and `/v/` only the version's MP4, so nothing else in either bucket, the D1 dumps and the inbox included, is reachable through them.

Kept for measurement, since `docs/Perf.md` and `docs/Risks.md` still use them: `/i2/*` for the second-bucket experiment, the idle probes and browser runs and their crons, `/api/seed` and `/debug/image/*` behind admin login. Removed: `GET /api/ryw`, an unauthenticated write built for a risk since closed, and `PUT /upload/*`, a stand-in for the presigned PUT that tests no longer need since they can put an object and drive the queue themselves.

Uploads are validated at presign: the album exists and is a day album, each path is a media name in that album with an extension the gallery accepts, a path with no `replaces` names no existing item, and a `replaces` names an item in the album whose base name the path keeps and the path names no item other than that one, so the admin who drops a PNG on `felix.jpg` beside an existing `felix.png` hears so before the upload, not after. The pipeline trusts nothing else: an inbox object with no `upload` row is left for the purge, and one whose row is complete is a redelivery, which deletes the object and does nothing else.

### Search

FTS5 over `item_name`, `title`, `description`, `tags` and `summary`, as now. The caller's terms become an FTS query by quoting each word, so no input is a syntax error. Results are sorted by gallery path, newest first unless `oldestFirst`, filtered to the years asked for, paged with `pageSize` and `startAt`, and `total` counts every match. Guests see published albums and the media in them, which AWS did not filter.

### The upload pipeline

The R2 event for `inbox/<versionId>` reaches the queue consumer, which: finds the `upload` row for that version, and stops if there is none, or deletes the object and stops if the row is complete; reads the file; for an image, reads its size, caption and keywords with ExifReader (`api/src/media/exif.ts`, which reads the IPTC fields a JPEG carries and the XMP ones a HEIC or PNG has), and for a video, has the container transcode it to `derived/<versionId>/video.mp4` and `poster.jpg` and report its size and duration; writes the original to `originals/<versionId>` with the path in its metadata; then in one batch inserts the item, or for a replacement updates the target row, creates the year and day albums if missing, makes the first upload into a day its thumbnail, clears any upload error for the path, and marks the upload complete; then generates the thumbnail and the detail image, the two sizes the album page and the media page are about to ask for, so the first reader never waits for a transformation, as AWS's upload Lambda did for the detail image; and last deletes the inbox object. That is two transformations per upload, a small fraction of the 5,000 a month the Images allowance includes. A replacement is not destructive: it sets the name, version, media type, size and duration, keeps the crop only if the dimensions are unchanged, and leaves an existing title, description and tags alone, taking the file's caption only where the row has none, `title = coalesce(item.title, ?)` in the `UPDATE`, where the prototype's upsert overwrote every column. A replacement whose target was deleted while the upload was in flight, an `upload` row with a `target_path` and no `target_id`, becomes an `upload_error` saying so, and the object is left for the purge; the name the pipeline finds taken by another item is the same kind of error. A file that is not what its name says becomes an `upload_error` row and is dropped, not retried. So does a HEIC the Images binding cannot decode: the thumbnail cut at the end is where that surfaces, so the admin sees the file named in the upload status rather than a broken image in the album.

## Headers and robots.txt

The site has no public presence: every page is `noindex`, AI training is refused, hotlinking is refused. On AWS that is CloudFront's work, in `tacocat-gallery-hosting-aws`'s `template.yaml`: a function that serves `robots.txt`, and two response-header policies. Here only one of those rules exists, the immutable `Cache-Control` on `/_app/immutable/*` in `web/static/_headers` and on derived images, and `docs/plans/Release.md` lists the rest as owed. The port carries them over:

- **`robots.txt`**: `User-agent: *` with `Allow: /`, so ordinary crawlers fetch pages and see the `noindex`, where a `Disallow` would hide it and leave bare URLs listed. The `Disallow: /` block for the AI training bots is Cloudflare's: the zone's managed robots.txt setting prepends its block, with a list longer than the CloudFront function's and kept current by Cloudflare, to whatever the origin serves at `/robots.txt` with a 200. The origin has to serve a real text file there, since the app's fallback would otherwise answer with `index.html` and Cloudflare would prepend to that. A static file in `web/static/`.
- **`X-Robots-Tag: noindex, noimageindex, nosnippet, max-image-preview:none, notranslate, noarchive, noai, noimageai`** on every response, which is how `noindex` reaches images and anything already indexed, since the app's meta tag covers only its own markup. And `tdm-reservation: 1`.
- **Security headers** on every response, with the template's values: `Strict-Transport-Security`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `X-Frame-Options: DENY`, and its `Permissions-Policy` with camera, geolocation, microphone and the rest turned off.
- **`Cross-Origin-Resource-Policy: same-site`** on `/i/*`, `/v/*` and `/raw/*`, so another site cannot embed the photos. AWS sends it in production only, because its local dev server loaded images from the staging image host and `localhost` is same-site with nothing; here the media routes are on the site's own origin in every environment, `wrangler dev` included, so the header is unconditional and staging's tests assert the real value.
- **Not carried over**: the image CDN's `Timing-Allow-Origin`, which let the app read full Resource Timing entries for thumbnails across origins. Same-origin timings are never withheld, so it has no job here.

The app shell needs nothing beyond the immutable rule already in `_headers`. AWS cached `index.html` under CloudFront's CachingOptimized policy, so a deploy had to invalidate it, and mapped 403 and 404 to `index.html` as custom error responses; Workers serves assets with `Cache-Control: public, max-age=0, must-revalidate` and an ETag, and `not_found_handling: single-page-application` is the error mapping.

`robots.txt` asks; the zone enforces. `infra/main.tf` already sets the zone's `ai_bots_protection` to block, which turns known AI crawlers away before they reach the Worker. The file is for crawlers that honour it, for whatever the zone's list misses, and for the record.

Two places serve responses, so the headers live in two places. The app's pages and files, the not-found fallback to `index.html` included, never reach the Worker, so theirs go in `web/static/_headers` as a `/*` block beside the immutable rule. Everything the Worker answers itself goes through the wrapper in `index.ts` that already adds `x-auth-status`, `x-worker-colo` and `server-timing` to every response, so it is one block there. Tests: an integration case asserting the set on an API response and an image response, a stack test asserting it on the app shell and on an app route's fallback, since the asset router serves those and that is the thing to confirm rather than assume, and one for `robots.txt`'s body.

## Layers

```text
api/src/index.ts   the composition root: fetch, queue and scheduled handlers, wired to the layers below
api/src/routes/    HTTP, on Hono: parse the request, gate on the admin, call gallery/, shape the response
api/src/gallery/   the operations: album and media reads and writes, search, the upload pipeline, the purge
api/src/auth/      passkeys, sessions, the login and invite pages
api/src/ops/       probes, browser runs, backup, health: measurement and operations, not the gallery
api/src/http/      responses: JSON and HTML, the error body, cookies, the bookmark header and cookie, path helpers
api/src/db/        schema, the ORM over D1 sessions, timestamps, the upsert helpers
api/src/storage/   R2 keys, presigning, listing
api/src/media/     ExifReader, the Images binding, the transcoder container
api/src/util/      helpers with no layer, such as running work in sequence
```

The rules, each an ESLint `no-restricted-imports` block as the web app's layers are, plus `import-x/no-cycle` over the Worker and `shared/`. The web app is left out of the cycle check: its album models cycle, as they did in the AWS app, and untangling that is a change to the app.

- `routes/` imports anything below it; nothing imports `routes/`.
- `gallery/` imports `db/`, `storage/`, `media/` and `shared/`, never `auth/`, `ops/` or `http/`. Who is asking arrives as a boolean; the transcoder is handed presigned URLs, since `media/` cannot make them.
- `auth/` imports `db/`, `http/` and `shared/`.
- `ops/` imports `gallery/`, `db/`, `storage/`, `http/` and `shared/`.
- `http/`, `db/`, `storage/`, `media/` and `util/` import `shared/` and not each other; every layer may import `util/`.
- Modules within a layer may import each other. Tests are exempt.

## The web app

Two changes, both because the platform differs, and the layering rules of the app hold. All of it is admin-only code, so the reader's path, which the performance comparison measures, is untouched.

1. **The presign request says what each upload is, and the response carries the version id.** `s3Upload.ts` sends `[{ path, replaces? }]` and reads `{ url, versionId }` per path instead of the `x-amz-version-id` response header, which R2 does not send. The app computes `path` for a replacement, the target's base name with the new file's extension, which is what `getUploadPathForReplacement` becomes and what the Worker checks. The album drop zone sends `replaces` for every colliding name, same extension included, since a path without it may name no existing item; `enrichWithPreviousVersionIds` already finds those names.
2. **Any file replaces any item, and completion is by version id.** `getReplacementExtensionError` goes. Completion becomes one rule for every case: after the refetch, the album holds a media item whose `versionId` is the one presign returned, found by scanning the album rather than by path, so the rule does not care whether the replacement renamed. After a replacement completes on a media page, the machine finds that item and navigates to its path, since the name may have changed, as `MediaRenameMachine` does after a rename.

With no rename on the server, the "renamed on server" concept that HEIC needed is dead code end to end, and step 10 removes it rather than leaving a branch that can never run:

- `fileFormats.ts`: `getMediaPath` and `isRenamedOnServer` go. What remains per format is two browser facts, `browserCanDisplay` and `getProcessingTimeout`, which is a small table, not a handler registry.
- `models/album.ts`: `UploadEntry` and `MediaItemToUpload` carry an `uploadPath` and a `mediaPath` that are now always the same, and every comment on them explains HEIC; they become one path. `previousVersionId` existed only because a converted HEIC's upload id said nothing about completion; it goes.
- `uploadUtils.ts`: `isUploadComplete` is one comparison, `findProcessedUploads` loses its renamed branch and the "check both paths" lookup, and `enrichWithPreviousVersionIds` shrinks to returning the colliding names for `replaces`.
- `UploadMachine.svelte.ts` stops calling `getMediaPath` when it builds entries.
- What stays: the HEIC mentions in `mediaValidation.ts` and `UploadThumbnail.svelte`, which are about whether the browser can preview a HEIC, true on any platform; and both rename machines, which post, refetch the parent and drop the old path, since AWS's copies and its size cap never reached the app.
- Optional, once the above is in: the `?fresh` flag and the per-album `albumChangedAt` bookkeeping in `AlbumState.svelte.ts` and `AlbumLoadMachine.svelte.ts` exist to get past CloudFront after a save. The bookmark cookie makes every read after a write consistent whether or not `?fresh` is sent, so `reloadAfterChange` can be a plain fetch and the bookkeeping can go. And `s3Upload.ts`, `uploadToS3` and the S3 comments in the models are misnamed now; rename them if the step touches those files anyway.

Everything else the Worker conforms to: the paths and bodies above are the app's, `errorMessage` is what it already reads, and its `?fresh` re-read after a save is answered by the bookmark cookie the write set.

## Tests

The AWS integration suites, seventeen of them against a deployed stack, become Worker integration tests against local bindings, one file per operation family, following `docs/Testing.md`. Worth carrying over from them: a per-suite fixture that builds an album with photos, a `waitFor` that polls to a deadline for the queue's effect, and the habit of one scenario per describe with the mutation in `beforeEach` and the observations in the tests. The rows-read budget tests grow a case for every new query. Admin journeys, edit, publish, create, rename, delete, set thumbnail, crop, get Playwright tests in `e2e/`; uploading through a presigned PUT has no local S3 endpoint, so its e2e test stops at the presign response and the pipeline is covered by the integration tests. Every commit below leaves `npm run quality` green.

## From AWS to the Worker

| AWS                                                           | Worker                                                                       |
| ------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| `GetAlbum`, `AlbumExists`, `MediaExists`                      | `GET`/`HEAD /api/album`, `HEAD /api/media`                                   |
| `GetLatestAlbum`                                              | Dropped; the app reads it off the current year                               |
| `Search` on RediSearch, `DynamoToRedis` stream, `SyncRedis`   | `GET /api/search` on FTS5 in the same database; the triggers keep it in step |
| `CreateAlbum`, `UpdateAlbum`, `DeleteAlbum`, `RenameAlbum`    | `PUT`, `PATCH`, `DELETE /api/album`, `POST /api/album-rename`                |
| `SetAlbumThumbnail`                                           | `PATCH /api/album-thumb`, by row id                                          |
| `UpdateMedia`, `DeleteMedia`, `RenameMedia`, `RecutThumbnail` | `PATCH`, `DELETE /api/media`, `POST /api/media-rename`, `PATCH /api/thumb`   |
| `GenerateUploadUrls`, `GetErrors`                             | `POST /api/presigned`, `POST /api/errors`                                    |
| `ProcessMediaUpload` on an S3 event                           | The queue consumer on an R2 event                                            |
| MediaConvert and `VideoTranscodingComplete`                   | The ffmpeg container, called from the same consumer                          |
| `GenerateDerivedImage` behind CloudFront                      | `GET /i/*` with the Images binding and the derived bucket                    |
| S3 versioning, `revertS3Version`, the 24-day expiry           | Write-once keys, a row that points only at what succeeded, the 30-day purge  |
| Cognito and `tacocat-gallery-auth`                            | Passkeys, unchanged                                                          |
| The errors table with a TTL                                   | `upload_error`, purged nightly                                               |
| AWS Backup of the originals                                   | Not in this port; see Risks #22                                              |

## Order of work

This plan, steps 2, 3 and 11 change no data and need no wipe, so each is its own pull request to `main` first, released to production as any change is; that keeps them out of the port's diff and shortens the time staging is half usable. The port itself is one pull request on a branch off `main`, starting at step 4, one commit per step below, each reviewed and okayed before the next starts, and each leaving `npm run quality` green and the push deploying. Staging may be only partly usable between steps, with no upload path between step 5 removing `/upload/` and step 8 adding presign, for one; nothing but tests uses staging, the measurements run against production, and the branch reaches production only when it is done. A session working a step reads this page and `docs/Testing.md` first, and the AWS repo's code for the operation it is porting, on `main`. It commits and does not push: pushing is Dean's call, since a push releases the branch head to staging, and the by-hand wipes go with the pushes rather than the commits, the database before the first push that carries step 4 and the buckets before the first that carries step 8.

The steps:

1. **This plan.** _Done: #27, 2026-09-24._
2. **Layers.** Files move into the directories above, the import rules and the cycle check go into `eslint.config.ts`, nothing changes behaviour. _Done: #26, 2026-09-24._
3. **Hono, the error shape and dispatch.** The `startsWith` chain in `index.ts` becomes a Hono app in `index.ts` and `routes/`, with the response wrapper and the admin gate as middleware; `{ errorMessage }` from `shared/` through `onError` and `notFound`; every method routed; exceptions no longer leak; the app's tests updated where they assert on a body. The routes themselves keep their paths and answers. _Done: #28, 2026-09-24._
4. **Schema baseline**, in two commits. First `api/scripts/check-gallery.ts`: it reads every album of the AWS gallery, from a DynamoDB export, or through the public API when there is no AWS access, which sees published albums only, since AWS checks who is reading; writes each row through `PUT /api/item` into a local `wrangler dev` running the baseline drafted in the working tree; and reports every refusal with its paths, whether the shared schema refused the body, which it does before any constraint sees the row, or a constraint refused it, by name. It is the first half of the gallery copy (see Afterwards), not a throwaway. Then the baseline as those findings shaped it: the migrations reset, timestamps on every table, the constraints, the thumbnail foreign key, the two indexes, the `upload` table; the staging database emptied by hand before the push. A constraint that turns out to reject real rows costs a table rebuild to change, which is why the check comes first.
5. **Reads.** `HEAD` for albums and media, search with the app's URL and shape, `/raw/` and `/v/` narrowed to a version's original and MP4, the seed and debug routes behind login, `/api/ryw` and `/upload/` removed.
6. **Album writes.** Create, update with the publish guard, delete, rename, set thumbnail.
7. **Media writes.** Update, delete, rename, recut.
8. **Storage.** Keys by version id, the id minted at presign and written to the `upload` table the baseline created, the presign route with its body and response, the media bucket's CORS policy in `infra/`, the pipeline reading the upload row, replacement in any format with the crop rule, the thumbnail and detail image generated at the end, the transcoder writing to the derived bucket, the metadata label, the media script. No migration. Staging's buckets are emptied by hand with this push, as its database was at step 4: every object under the old path keys is garbage the moment this deploys, more than the purge's per-run cap would clear in weeks of nights. Production's buckets get the same at step 12.
9. **The purge**, and the backup written to match.
10. **The web app**: the two changes, the removal of the renamed-on-server code listed with them, the optional items if they fit, and the e2e admin journeys.
11. **Headers and `robots.txt`**, as above. Independent of every other step and its own pull request, so it can go first if a session wants a small one. _Done: #29, 2026-09-24; the `_headers` file does reach an app route's fallback, which the stack test confirms. The bot list came out of `robots.txt` again once the zone's managed robots.txt turned out to prepend Cloudflare's own. The derived buckets' public image hosts never pass through the Worker, so they get the media half of the set from a zone rule in `infra/main.tf`, its own pull request._
12. **Docs, and the merge.** `docs/Architecture.md` written from the code, `README.md`, `docs/Risks.md` rows #5 and #13 updated, `docs/AGENTS.src.md` no longer calling this a prototype, `docs/plans/Release.md` no longer owing the headers. Before the merge, production's database and both its buckets are emptied by hand, the database backed up first as at step 4, since every object in them is under the old keys and the purge's cap would take weeks to clear them. Expect the merge's Deploy run to show as failed for the reason given under the migrations reset, and watch it.
13. **Production's gallery back.** Production is what the probes and the browser runs measure four times a day, and after the merge it is empty, so this comes the same day: the albums `docs/Perf.md` measures are imported again with `import-album.ts`, now writing rows through `PUT /api/item`. Every version id and every URL changes with the keys, so the edge cache and every browser cache start cold, and `docs/Perf.md` says so, dates it, and marks the runs on either side of the gap.

### What the first steps taught

For the sessions doing the rest, things a fresh session would otherwise find out the hard way:

- Branch from `main` after the previous step merges, and never `git stash` mid-step: a step's new files are untracked until staged, and a stash drops them from the tree.
- A step is one commit, apart from step 4, whose check lands before the baseline its findings shape, and the pre-commit hook runs every suite the staged files touch, so a commit takes about 40 seconds. Commit once, when the step is green, rather than in pieces.
- The test helpers to reach for are `call`, `callAsAdmin` and `parseExactly` in `api/test/helpers.ts`. A stack test can import a constant from `api/src` to hold two copies of something together, as the headers test holds `web/static/_headers` to the Worker's values.
- What Hono does with `HEAD`, a mid-path `*` and a guest's write is under The API above; read it before adding a route.
- `import-x/no-cycle` sees `.ts` files only because the config names the extension; the comment there says so, so it is not a setting to tidy away.
- Wrangler's local Durable Object reset prints `Application called deleteAllDurableObjects()` as an uncaught exception during the tests. It is the Vitest Workers plugin's storage isolation, not a failing test.

## Afterwards: copying the gallery

A later plan, but the design above is shaped for it, and one part of it comes first: **before the schema baseline lands, `check-gallery.ts` at step 4 runs the constraints over the production gallery's data.** A `CHECK` that rejects real rows costs a table rebuild to change, and there are reasons to expect some: AWS's image type had `dimensions` optional and its repo carries a dimensions-repair migration with an "unfixable" count, so some rows may lack a size; blank captions, an unlisted video extension, or a name outside AWS's own rule of letters, digits, underscore, hyphen and one extension, which came long after most of the uploads, are all possible in 25 years of them. A scan of the production table is under a penny by the AWS repo's `docs/Migrations.md`, or failing AWS access, every published album through the public API, which is most of the gallery but not the drafts. The copy then repairs rows, reading a missing size from the original, rather than the schema bending later.

Each current original in the AWS bucket is copied to `originals/<its S3 versionId>` with its path in the metadata. S3 documents its version ids as opaque, and its own example contains `/` and `+`, which the URL parser and the `version_id` constraint would both refuse; a sample of 76 ids from three production albums on 2026-09-24 were all 32 characters of letters, digits, `.` and `_`, so the copy checks every id against both and mints a new one for any that fails. Copied ids carry no timestamp, which is why the purge judges age by R2's upload time rather than by the id. The rows come from a DynamoDB export written through `PUT /api/item`, with `itemType` and `mediaType` finished on the way; media rows go in before any album's thumbnail is set through `PATCH /api/album-thumb`, since the foreign key needs the media row to exist. The derived bucket is not copied: derivatives regenerate on request. The observed rate, about 7,700 distinct variants a month and 7,100 without bots, is above the 5,000 the Images allowance includes, so expect about $1 to $1.35 a month at $0.50 per 1,000 after the copy, which the Spending rules want said out loud; a backfill of the newest albums, if the first readers should not wait, is more of the same. `docs/Migrations.md` in the AWS repo has the lessons that apply: idempotent, resumable, newest albums first, diagnose before fix.
